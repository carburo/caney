{% extends 'base_bare.html.twig' %}

{% block content %}
    <section class="container-fluid">
        <div class="row">
            <div class="col-md-3 sidebar">
                {{ knp_menu_render('AppBundle:Builder:user2Menu', {
                    'currentClass': 'active',
                    'menu_template': 'SonataBlockBundle:Block:block_side_menu_template.html.twig'
                }) }}
            </div>

            <div class="col-md-9">
                <div class="row">
                    <div class="col-xs-12">
                        <h2 style="text-align: center">{% block headingTitle %}{% endblock %}</h2>
                    </div>
                </div>
                {% block fos_user_content %}{% endblock %}
            </div>
        </div>
    </section>
{% endblock %}